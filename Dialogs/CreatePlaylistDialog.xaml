<Window x:Class="MusicPlayer_by_d3solat1on.Dialogs.CreatePlaylistDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создать плейлист"
        Height="450"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название плейлиста -->
        <TextBlock Grid.Row="0"
                Text="Название плейлиста:"
                FontSize="14"
                Margin="0,0,0,5"/>
        <TextBox x:Name="PlaylistNameTextBox"
                Grid.Row="1"
                FontSize="16"
                Padding="5"
                Margin="0,0,0,15"/>

        <!-- Описание -->
        <TextBlock Grid.Row="2"
                Text="Описание:"
                FontSize="14"
                Margin="0,0,0,5"/>
        <TextBox x:Name="PlaylistDescriptionTextBox"
                Grid.Row="3"
                FontSize="14"
                Padding="5"
                 TextWrapping="Wrap"
                AcceptsReturn="True"
                Height="80"
                Margin="0,0,0,15"/>

        <!-- Обложка -->
        <Border Grid.Row="4"
                BorderBrush="Gray"
                BorderThickness="1"
                Margin="0,0,0,15"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
            <Grid>
                <Image x:Name="CoverImage"
                        Width="200"
                        Height="200"
                        Stretch="UniformToFill"/>
                <Button x:Name="SelectCoverButton"
                        Content="Выбрать обложку"
                        Width="120"
                        Height="30"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Click="SelectCoverButton_Click"
                        Background="#EEEEEE"/>
            </Grid>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="5"
                Orientation="Horizontal"
                HorizontalAlignment="Right">
            <Button x:Name="CreateButton"
                    Content="Создать"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    Click="CreateButton_Click"
                    IsDefault="True"/>
            <Button x:Name="CancelButton"
                    Content="Отмена"
                    Width="100"
                    Height="35"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>